import PendingSection from "@/components/pending/PendingSection";

export const metadata = {
  title: "Dashboard | Pending Requests",
  description: "Track and manage all pending requests in one place. Review, approve, or take action to keep your workflow moving smoothly.",
};

export default function Page() {
  return <PendingSection />;
}